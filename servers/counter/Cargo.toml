[package]
name = "tanix-counter"
version.workspace = true
edition.workspace = true
description = "Tanix Phase-8 demo app \"counter\" — a +/− button window (Phase 8)"

[dependencies]
tanix-libsys = { path = "../libtanix-sys" }
tanix-libtanix-ui = { path = "../libtanix-ui" }
