[package]
name = "tanix-libtanix-ui"
version.workspace = true
edition.workspace = true
description = "Shared UI helpers for Tanix apps: window canvas + 5x7 bitmap font (Phase 8)"

[dependencies]
tanix-libsys = { path = "../libtanix-sys", features = ["alloc"] }
