[package]
name = "tanix-hog"
version.workspace = true
edition.workspace = true
description = "Tanix Phase-7 demo — a lowest-priority CPU hog proving preemption"

[dependencies]
tanix-libsys = { path = "../libtanix-sys" }
