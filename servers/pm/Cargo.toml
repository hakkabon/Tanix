[package]
name = "tanix-pm"
version.workspace = true
edition.workspace = true
description = "Tanix process manager — spawn/kill services (Phase 4)"

[dependencies]
tanix-libsys = { path = "../libtanix-sys" }
