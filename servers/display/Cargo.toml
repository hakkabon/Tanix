[package]
name = "tanix-display"
version.workspace = true
edition.workspace = true
description = "Tanix display server — owns virtio-gpu + virtio-tablet (Phase 5)"

[dependencies]
tanix-libsys = { path = "../libtanix-sys" }
