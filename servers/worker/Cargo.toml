[package]
name = "tanix-worker"
version.workspace = true
edition.workspace = true
description = "Tanix demo worker process — spawned by pm on request (Phase 4)"

[dependencies]
tanix-libsys = { path = "../libtanix-sys" }
