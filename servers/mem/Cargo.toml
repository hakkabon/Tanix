[package]
name = "tanix-mem"
version.workspace = true
edition.workspace = true
description = "Tanix memory server — frame allocation as a service (Phase 4)"

[dependencies]
tanix-libsys = { path = "../libtanix-sys" }
