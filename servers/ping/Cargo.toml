[package]
name = "tanix-ping"
version.workspace = true
edition.workspace = true
description = "Tanix Phase-12 cross-CPU IPC stress — ping side of a ping/pong pair"

[dependencies]
tanix-libsys = { path = "../libtanix-sys" }
