[package]
name = "tanix-clock"
version.workspace = true
edition.workspace = true
description = "Tanix Phase-8 demo app \"clock\" — animated analog clock window, SYS_SLEEP demo (Phase 8)"

[dependencies]
tanix-libsys = { path = "../libtanix-sys" }
tanix-libtanix-ui = { path = "../libtanix-ui" }
