[package]
name = "tanix-pong"
version.workspace = true
edition.workspace = true
description = "Tanix Phase-12 cross-CPU IPC stress — pong side of a ping/pong pair"

[dependencies]
tanix-libsys = { path = "../libtanix-sys" }
